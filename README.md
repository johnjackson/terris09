0.90版更新	2006.6.7 2:15am
修改了平移的BUG，
“落底”固定计时也改好了。


0.88版更新	2006.6.6 23:20pm
  修改了下落时的碰撞判定代码，不再使用最大高度（TOP值），改为以grid值来判定。为接下来的修改平移BUG做准备。现在“落底”后并不立刻固定，但还要改进。


0.85版更新	2006.4.28 22：48PM

  更正了消层部分的代码，现在已经可以正常同时消除多层


   还存在到高层后出现不消层现像，(top值BUG）

还有一些非基本功能要改正：

    变形的代码还有BUG要改
    比如下落后还要过一段时间在固定，(已改)
    每个形状变形后的位置，如竖变形不是以最中心为轴的。 现在（都是左对齐，要改为居中）
    现在没有采用WM_KEYDOWN消息处理键盘，用的是宏（即时），所以有时按下没反应，有时按一下动两下


有待加入的功能：

    计分系统，(已加）
    难度级别系统，(easy to do)
    下一方块提示系统，
    还应该再加两种L型方块